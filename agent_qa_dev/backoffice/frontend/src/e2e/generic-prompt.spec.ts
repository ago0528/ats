import { describe, expect, it } from 'vitest';

describe('generic-prompt e2e placeholder', () => {
  it('keeps test runner green until browser e2e is wired', () => {
    expect(true).toBe(true);
  });
});
